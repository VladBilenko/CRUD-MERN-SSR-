const NotationTag = '[NOTATION]';

const NotationActionTypes = {
    LoadAll: `${NotationTag} Load all`,
    SetAll: `${NotationTag} Set all`,
    LoadOne: `${NotationTag} Load one`,
    SetOne: `${NotationTag} Set one`,
    Create: `${NotationTag} Create`,
    Update: `${NotationTag} Update`,
    Delete: `${NotationTag} Delete`,
    ClearData: `${NotationTag} Clear data`
};

export default NotationActionTypes;